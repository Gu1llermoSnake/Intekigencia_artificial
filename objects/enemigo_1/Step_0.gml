/// @description Inserte aquí la descripción
// Puede escribir su código en este editor
if (instance_exists(player))
{
    move_towards_point(player.x, player.y, spd);
	}
image_angle= direction;
if (hp) <= 0
{
    with(SCORE) thescore = thescore + 5;
    instance_destroy();
}